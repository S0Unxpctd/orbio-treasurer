# T-001 · Monorepo scaffold, tooling, CI

Day: 0 · Layer: L0 · Estimate: 2h · Depends on: — · PRD: ARCH §1-2, §9

## Goal
pnpm/Turborepo monorepo with core, create-orbio-agent, web, book-daily, scripts; Biome, Vitest, typecheck; GitHub Action on PR.

## In scope
- pnpm workspaces + turbo.json
- packages/core, packages/create-orbio-agent, apps/web (Next.js 15), apps/book-daily (empty), scripts/probes
- Biome, tsconfig strict, Vitest
- GitHub Action: install, lint, typecheck, test
- Root scripts = the commands in CLAUDE.md, incl. `pnpm probe <P-n>`

## Out of scope
- Product code
- Supabase project (T-002)

## Acceptance criteria
1. `pnpm install && pnpm lint && pnpm typecheck && pnpm test` pass on a clean clone
2. CI green on first PR
3. Every command in CLAUDE.md exists

## Audit focus
- Extra dependencies beyond ARCH §1
- tsconfig not strict

## Tests required
- smoke: CI run link

## Status
done (So ok 2026-09-09; AC2 CI link still to paste — repo pushed to GitHub by So on 2026-09-08, first run not yet visible from the sandbox)

## Build notes
_(builder, 2026-09-08)_
- pnpm 10.28 workspaces + Turborepo 2.10; packages: `@orbio-treasurer/core`, `create-orbio-agent`, `@orbio-treasurer/web` (Next 16.3, App Router, `/api/health` route with `runtime='nodejs'`), `@orbio-treasurer/book-daily` (depends on core via `workspace:*`), `scripts/probe.ts` runner + `scripts/probes/README.md`.
- Tooling: Biome 2.5 (format + lint, `noConsole` error in core with `console.error` allowed; off in scripts/CLI/web), TypeScript 5.9 strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` (`tsconfig.base.json`), Vitest 4 per package (`--passWithNoTests` for empty packages), `.github/workflows/ci.yml` (install --frozen-lockfile, lint, typecheck, test).
- Root scripts match CLAUDE.md: dev, build, test, test:policy, lint, lint:fix, typecheck, smoke (stub until T-019), db:migrate, tick (stub until T-014), probe.
- `pnpm-workspace.yaml` allowlists esbuild's postinstall (`onlyBuiltDependencies`) so installs are non-interactive.
- Verified locally: `pnpm install && pnpm lint && pnpm typecheck && pnpm test` green; `next build` of apps/web succeeds (routes `/`, `/api/health`).
- **Fixes after audit/test pass 1 (2026-09-08):** root `"type": "module"` so `scripts/probe.ts` runs as ESM (`pnpm probe P-1` → exit 2 with "no probe script…", `pnpm probe` → usage, exit 2); `test:policy` now `exec vitest run src/policy --passWithNoTests` (filter honoured: "No test files found" until T-015); `scripts/` typechecked via `scripts/tsconfig.json` (NodeNext) chained into root `typecheck`; turbo `test` task no longer declares `coverage/**` outputs (warning gone); ADR-004 records Next 16 + `tsx`; ADR-001 cross-references it. Left as-is on purpose (Minor, noted): `/api/health` + placeholder page kept as deploy smoke target for T-022; `export type Layer` kept; `moduleResolution: Bundler` in packages revisited in T-011 when core gets real imports.

## Discovered
- Next.js current major is 16 (ARCH said 15); recorded in ADR-004 after the pass-1 audit; ARCH §1 points to it.
- Biome 2.5 prints a deprecation *info* for `rules.recommended` (they want a future `preset` key that this version doesn't accept yet). Exit code 0; harmless; revisit when Biome 3 ships.
- Vitest 5 / TypeScript 7 exist; pinned to 4.x / 5.9 (ARCH says exact versions preferred — leaving majors alone this week).

## Blocked on
—

## Audit report
_Pass 1 (2026-09-08, fresh session): 1 Blocker (probe runner CJS/top-level-await crash), 2 Major (test:policy filter ignored; ARCH edited without ADR), 9 Minor, 2 Questions. Full text in `tasks/reports/T-001-audit-1.md`. → in-code. Pass 2 (delta 8cba144): Blocker + both Majors verified fixed on a clean clone, no regression; 2 Minors (ARCH pointer to ADR-004 — fixed; stale Discovered line — fixed), 1 Question for So (ratify ADR-004's rule that version bumps of listed deps need only a Discovered note). Deferrals accepted. → in-test._

## Test report
_Pass 1 (2026-09-08, fresh session, clean clone): 14 checks, 13 PASS, 1 FAIL (`pnpm probe` crash). AC2 (CI run) untestable until first push. Full text in `tasks/reports/T-001-test-1.md`. → in-code. Pass 2 (clean clone at 8cba144): 13/13 PASS incl. `pnpm probe P-1` → exit 2 with message, `pnpm probe` → usage exit 2, `test:policy` filter honoured, scripts/ typechecked. → done pending CI run link._

## Evidence
- `pnpm lint` → `Checked 26 files … Found 1 info.` exit 0
- `pnpm typecheck` → `Tasks: 5 successful, 5 total`
- `pnpm test` → core `1 passed`; other packages `No test files found, exiting with code 0`
- `pnpm --filter @orbio-treasurer/web build` → routes `/`, `/_not-found`, `ƒ /api/health`
- Repo pushed by So on 2026-09-08 to https://github.com/S0Unxpctd/orbio-treasurer (main @ a590b8c, sandbox fetch confirms). CI run link: _to paste — GitHub API not reachable from the sandbox until the repo is added as a session source_

## Sign-off
_(So: date + "ok" or what to change)_
2026-09-09 · So: **ok** (via chat, "Ok pour tout"). Recorded by the orchestrator.
Needed from So: (a) `ok` on this ticket; (b) ratify or amend ADR-004's rule on version bumps; (c) push to GitHub and paste the first CI run link into Evidence.
