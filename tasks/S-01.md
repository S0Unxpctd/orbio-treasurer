# S-01 · Gateway + router (`/v1/chat/completions`, `model: "auto"`)
Sprint 1.0 · Layer: L0 · Depends on: — · PRD: docs/PRD-1.0-sprint.md §3, §4 T-1, §6

## Goal
Any OpenAI-compatible client that changes only `base_url` and sends `model: "auto"` gets a completion served by the cheapest Orbio model that fits, with the real cost and the baseline cost captured per call.

## In scope
- `apps/web/app/v1/chat/completions/route.ts` (POST) and `apps/web/app/v1/models/route.ts` (GET). Node runtime, not edge.
- Caller auth: `Authorization: Bearer otk_<32 hex>`. For this ticket the valid keys come from env `GATEWAY_KEYS` (comma-separated `otk_…` values, hashed with sha256 at boot and compared by hash). A `CallerKeyStore` interface (`lookup(hash) → {keyId, agentId|null} | null`) is defined in `packages/core/src/router/keys.ts` with the env implementation; the DB implementation lands in S-02/S-06. Missing/unknown key → 401 JSON `{error:{type:"auth", message}}`.
- Router: `packages/core/src/router/` — pure `route(input, catalog, opts) → {model, tier, reason}` with NO I/O. Inputs: the request body (messages, tools, response_format, requested model), the model catalog (from `/models`, cached ≤10 min by the route handler), floor from `auto:S|M|L`, and `mode` (`normal|eco|critical`, default `normal`) from an env `TREASURER_MODE` for now.
  - Tiers from per-token input price in the catalog: S ≤ $0.40/M, M ≤ $3/M, L above. Defaults per tier = cheapest model in the tier among an allow-list env `ROUTER_ALLOW` (comma-separated ids; if unset, all).
  - Rules in order: `tools` present or `response_format` json → ≥ M; total prompt chars > 24 000 → ≥ M; system prompt matches /\b(reason|analy|code|plan)/i → ≥ M; else S. Then apply floor (`auto:X`), then policy cap (`eco` caps at M, `critical` caps at S; cap wins over floor and the reason says so).
  - Exact Orbio model id in `model` → pass through untouched (`tier` derived, `reason: "explicit"`).
  - Baseline model for savings: header `x-baseline-model` if present in the catalog, else tier-L default. `baselineCostUsd = tokens × baseline prices`.
- Upstream: forward to `${ORBIO_GATEWAY_BASE_URL}/chat/completions` with the Orbio key from env `ORBIO_KEY` (wallet-signed key derivation lands in S-03; keep a `getUpstreamKey()` seam). Streaming (`stream:true`) passes SSE through unchanged and reads the final `usage` chunk; non-stream reads `usage.cost` and `usage.*_tokens`. Response headers added: `x-treasurer-model`, `x-treasurer-tier`, `x-treasurer-reason`, `x-treasurer-cost-usd`, `x-treasurer-baseline-usd`.
- Every call → `recorder.record(CallRecord)` where `CallRecorder` is an interface in `packages/core/src/router/recorder.ts` with an in-memory implementation and a JSONL-to-stdout implementation (redacted). Fire-and-forget, one retry, never blocks the response. The ledger-backed recorder is S-02/S-06.
- Upstream 402/insufficient balance → 503 `{error:{type:"treasury_empty"}}`; upstream 5xx → 502 with the upstream status in the body; upstream body parsed with Zod (unknown extra fields tolerated; missing `usage` on non-stream → `AdapterShapeError`, response still returned to the caller, record marked `status:"no_usage"`).
- `GET /v1/models` proxies the Orbio catalog and adds `auto`, `auto:S`, `auto:M`, `auto:L` entries.
- Env additions in `packages/core/src/env.ts`: `GATEWAY_KEYS`, `ROUTER_ALLOW`, `TREASURER_MODE`. Update `docs/runbook.md` with a curl example.

## Out of scope
- Ledger writes, DB keys (S-02, S-06). Wallet-signed key (S-03). Policy/tick (S-06). Public page (S-08). Rate limiting. Caller billing.

## Acceptance criteria
1. `route()` is pure (no imports of fs/net/Date) and has table-driven tests covering: S/M/L classification from prices, each rule, floor, cap-over-floor, explicit model pass-through, unknown explicit model → error, baseline selection. ≥ 12 cases.
2. `POST /v1/chat/completions` without a key → 401; with a valid key and `model:"auto"` and a 5-word prompt → routed to tier S (asserted via `x-treasurer-tier`), response body is a valid OpenAI chat completion.
3. Same with `stream:true` → SSE chunks pass through and the recorder still receives a record with `costUsd` when the upstream sends a usage chunk.
4. `x-treasurer-cost-usd` equals upstream `usage.cost` (string, 6 decimals) and `x-treasurer-baseline-usd` = tokens × baseline prices from the catalog (test with a fake catalog).
5. `GET /v1/models` lists Orbio models plus the four `auto*` ids.
6. No secret in any log line or response (grep the diff; `redact()` on upstream error bodies).
7. Live check (only if `ORBIO_KEY` has balance; otherwise mark "not run: balance 0" in Evidence): one real call through the local server, `cost_usd > 0` in the recorder output.

## Audit focus
Key material in logs/headers; SSE passthrough not buffering the whole stream; `usage` parsing on stream end; 402 mapping; cap-over-floor; catalog cache staleness; Zod on upstream shapes.

## Tests required
- unit: `packages/core/src/router/route.test.ts` (table-driven), `keys.test.ts`, `baseline.test.ts`.
- integration: `apps/web/app/v1/chat/completions/route.test.ts` against a local fake upstream (an in-process `http` server replaying a recorded non-stream and a stream fixture under `packages/core/src/router/fixtures/`, dated 2026-09-19).

## Status
in-test

## Build notes
- `packages/core/src/router/`: `types.ts`/`pricing.ts` (shared tier math), `route.ts` (pure
  `route()` — classification rules, `auto:X` floor, mode cap with cap-over-floor, explicit
  pass-through, `ROUTER_ALLOW` with nearest-tier fallback), `baseline.ts` (baseline model +
  cost), `keys.ts` (`CallerKeyStore`/`EnvCallerKeyStore`/`authenticateBearer`, sha256-hashed
  `GATEWAY_KEYS`), `recorder.ts` (`CallRecorder`/`InMemoryCallRecorder`/`JsonlStdoutCallRecorder`/
  `recordFireAndForget`), `errors.ts` (router-local `AdapterShapeError`), `catalog.ts` (Zod-
  validated `/models` fetch + ≤10 min TTL cache + the four `auto*` entries), `upstream.ts`
  (`forwardChatCompletion` — 402→`treasury_empty`, 5xx→`upstream_error`, non-stream `usage`
  parsing, SSE `.tee()` passthrough reading the final `usage` chunk in the background).
- `apps/web/app/v1/_gateway.ts`: per-request-agnostic wiring (env, key store, recorder, ≤10 min
  per-base-URL catalog cache) shared by both routes; a leading `_` keeps Next's router from
  treating it as a route.
- `apps/web/app/v1/chat/completions/route.ts` (POST, `runtime = 'nodejs'`) and
  `apps/web/app/v1/models/route.ts` (GET): auth → parse → catalog → `route()` → forward →
  `x-treasurer-*` headers → `recordFireAndForget`.
- Deviation (documented in `route.ts`): `x-treasurer-cost-usd`/`-baseline-usd` are only set on the
  non-stream response — for `stream: true` the final `usage` isn't known until the SSE body ends,
  after headers must already be sent, so those two headers are omitted on the streaming path. The
  call is still fully recorded (AC3 only requires the recorder receive `costUsd`, which it does).
- Fixtures: `packages/core/src/router/fixtures/{models-catalog,chat-completion.nonstream,
  chat-completion.stream}.2026-09-19.{json,sse}` — hand-written, OpenAI/Orbio-shaped, marked
  `_synthetic`/commented as such; never call the real gateway (see fixtures/README.md).
- Test-only auth/upstream secrets use the `sk-or-v1-TESTONLY…` convention (PROCESS.md §4).

## Discovered
- `packages/core`'s `build` script (`tsc -p tsconfig.json`) never copied
  `src/ledger/sqlite/schema.sql` into `dist/`. `SqliteLedgerStore` reads it via
  `readFileSync(new URL('./schema.sql', import.meta.url))` at module load, so importing
  `@orbio-treasurer/core` as a *built* package (which apps/web's `/v1/*` routes are the first
  consumer of — nothing outside `packages/core` depended on it before) crashed immediately on
  import. Fixed here (`package.json`: `build` now also copies the file into `dist/ledger/sqlite/`)
  because it blocked S-01 outright, not out of scope creep into ledger territory — no ledger
  *logic* touched. Flagging for whoever next builds/deploys `apps/web` against a real Postgres
  ledger (S-02/S-06) to double check `postgres/` has no equivalent gap (it doesn't: no
  `readFileSync` in that module).
- `packages/core/src/index.ts` didn't export `redact()`/`log()` despite CLAUDE.md #4 requiring
  every consumer to use `redact()` in log lines. Added both to the barrel (S-01's route handlers
  need them for AC6); no behavior change to either function.
- `docs/PRD-1.0-sprint.md` §4 T-1 and this ticket disagree slightly on the upstream key source
  (§4: "Treasurer key derived from `TREASURER_PRIVATE_KEY`"; ticket: "the Orbio key from env
  `ORBIO_KEY`... keep a `getUpstreamKey()` seam"). Followed the ticket (ticket wins for this
  sprint's explicit "In scope" text, and it names the seam S-03 replaces) — flagging per CLAUDE.md
  #2 since PRD normally wins on conflict.
- `ORBIO_GATEWAY_BASE_URL` has no default in `env.ts` (pre-existing, not added by S-01). Both
  routes fail closed with a safe 500 `{error:{type:"config"}}` rather than guessing a default.

## Blocked on
(none)

## Audit report
N/A — Sprint 1.0 process is Code → Test for this ticket; Audit is added only on S-04–S-07 (money
tickets), per the CLAUDE.md banner.

## Test report
- Unit (table-driven): `route.test.ts` 27 tests (S/M/L classification incl. both boundaries, all
  4 rules incl. non-trigger cases, `auto:X` floor, cap-over-floor incl. reason string, explicit
  pass-through, unknown-model error, `ROUTER_ALLOW` incl. nearest-tier fallback and the
  all-excluded error) — well over the ≥12 required. `baseline.test.ts` 9 tests. `keys.test.ts` 16
  tests.
- Integration (in-process fake upstream, `apps/web/test/fake-upstream.ts`): `chat/completions/
  route.test.ts` 12 tests, `models/route.test.ts` 2 tests.
- `pnpm lint && pnpm typecheck && pnpm test` all green (see Evidence).

## Evidence
- Test counts before → after this ticket: core `776 passed | 51 skipped` (17 files) →
  `831 passed | 51 skipped` (20 files); apps/web `0 tests` → `14 passed` (2 files). Full `pnpm
  test` run: `19 passed | 1 skipped` core test files / `831 passed | 51 skipped` tests, apps/web
  `2 passed` files / `14 passed` tests, book-daily and create-orbio-agent unchanged (no test
  files). `pnpm lint`: 0 errors (1 pre-existing warning + 1 pre-existing info, both in files this
  ticket didn't touch: `biome.json`'s deprecated `recommended` field, and
  `scripts/probes/p1-mcp-auth.ts`'s `ORBIO_MCP_URL` turbo-env warning). `pnpm typecheck`: 5/5
  packages pass.
- AC1 (route() pure, ≥12 table-driven cases): met —
  `packages/core/src/router/route.test.ts` (27 cases) + a source-scan test asserting no
  `node:fs`/`node:net`/`Date` import in `route.ts`.
- AC2 (401 without key; valid key + `model:"auto"` + 5-word prompt → tier S, valid completion
  body): met — `apps/web/app/v1/chat/completions/route.test.ts` describe blocks "auth (AC2)" and
  "non-stream, tier S (AC2)".
- AC3 (`stream:true` SSE passthrough + recorder receives `costUsd`): met —
  `route.test.ts` "stream: true passthrough (AC3)": asserts the response body byte-equals the SSE
  fixture and the `InMemoryCallRecorder` receives `costUsd: 0.000567`.
- AC4 (`x-treasurer-cost-usd` = upstream `usage.cost`, 6 decimals; baseline-usd = tokens ×
  baseline prices): met — `route.test.ts` "cost/baseline headers (AC4)", 3 tests incl. an
  explicit `x-baseline-model` header.
- AC5 (`GET /v1/models` lists catalog + `auto*`): met — `apps/web/app/v1/models/route.test.ts`.
- AC6 (no secret in logs/responses): met — `route.test.ts` "no secret in logs or responses (AC6)"
  spies on `console.error` and the response body during a 5xx path with a `sk-or-v1-TESTONLY…`
  `ORBIO_KEY` and asserts it never appears in either; also a manual grep of the full diff for
  `sk-[A-Za-z0-9_-]{15,}` / `0x[0-9a-fA-F]{64}` outside `TESTONLY` fixtures found nothing.
- AC7 (live check): **not run: balance 0.** No `.env.local` and no funded `ORBIO_KEY` in this
  worktree/sandbox — there is no live Orbio balance to call through. Left for So to run once a
  real `ORBIO_KEY` with balance is available (`curl` example added to `docs/runbook.md`).

## Sign-off
