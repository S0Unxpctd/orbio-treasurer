# T-011 · LedgerStore interface + SQLite and Postgres implementations

Day: 1 · Layer: L0 · Estimate: 4.5h · Depends on: T-002 · PRD: FR-1.0, FR-1.1, FR-1.2, FR-1.3, PRD §9, ADR-002

## Goal
Insert-only repositories behind one interface, two stores, one shared test suite; snapshot metrics math.

## In scope
- ledger/types.ts (LedgerStore), ledger/sqlite/*, ledger/postgres/*
- computeSnapshotMetrics(inputs) pure
- Decimal handling at the boundary (no floats in storage)
- Shared conformance test suite run against both

## Out of scope
- External reads

## Acceptance criteria
1. Conformance suite green on both stores
2. Table tests: zero burn, zero accrual, accrual>burn (runway null=∞), ε floor, low_confidence < 6h
3. Snapshot round-trip byte-identical on money fields (6 dp) in both stores
4. Kit config with LEDGER=sqlite and no SUPABASE_* boots

## Audit focus
- Float drift
- Dialect-specific behaviour leaking above the interface
- UTC everywhere

## Tests required
- unit: metrics.test.ts; integration: ledger-conformance.test.ts ×2

## Status
in-test

## Build notes

Files added (all under `packages/core/src/ledger/`, plus one export-list edit):
- `types.ts` — `LedgerStore` interface + per-table `New*`/`*Row` types, hand-kept in sync with
  `schema.ts` (same discipline `gen-sql.ts` uses for the SQL dialects). Only `agents` and
  `orders` get an `update*` method, typed to exactly FR-1.1's mutable-column allow-list; every
  other table gets only `insertX`. `key_meta` revocation is `insertKeyMeta` with `revokedAt`
  set — no update method exists for it at all.
- `decimal.ts` — exact decimal-string arithmetic (parse/format/add/sub/max/divide) on `bigint`
  scaled by 10^6, matching `numeric(18,6)`. Division rounds half-away-from-zero at 6dp. This is
  the one place any arithmetic happens on a money value; everywhere else it's an opaque string.
- `metrics.ts` — `computeSnapshotMetrics(input)`, pure (FR-1.3, FR-3.3): `coverageRatio` (null
  when nothing was spent), `runwayDays` (null = ∞ when accrual ≥ burn, else
  `credits / max(netBurn, ε)`), `burnLowConfidence` (`historyHours < 6`).
- `util.ts` — `newId()` (client-side UUID, used by *both* stores so id generation is never a
  dialect-specific behaviour) and `assertUtcIso()` (rejects any business timestamp not ending
  in "Z").
- `sqlite/store.ts` — `SqliteLedgerStore` + `openSqliteLedger(path)`: creates the parent dir,
  opens the file, sets `PRAGMA foreign_keys = ON`, applies `sqlite/schema.sql` (idempotent,
  `IF NOT EXISTS`) on every open. Uses `better-sqlite3`'s `RETURNING *` so every write returns
  the persisted row in one round trip.
- `postgres/store.ts` — `PostgresLedgerStore` + `openPostgresLedger(connectionString)`. Does
  **not** apply the schema itself (that's `pnpm db:migrate` / Supabase's job — see file header).
  Every mapper explicitly normalizes timestamps with `toIso`/`toIsoOrNull` rather than trusting
  postgres.js's Date-vs-string parsing, so the `IsoTimestamp = string, UTC` contract holds no
  matter what the driver hands back.
- `conformance-suite.ts` — `defineLedgerConformanceSuite(label, openStore)`: one shared set of
  ~19 tests (agents round-trip/update/not-found, key_meta append pattern, FK enforcement, money
  byte-identical at 6dp, usage_events/decisions/book_snapshots round-trips, orders
  insert→fill lifecycle with immutable-field checks, UTC rejection). Not a `*.test.ts` file
  itself — invoked once per dialect.
- `ledger-conformance.test.ts` — runs the suite against `openSqliteLedger(':memory:')`
  (always) and against Postgres only when `TEST_DATABASE_URL` is set, mirroring
  `schema.postgres.test.ts`'s drop-and-reapply-migrations `beforeAll` exactly. No network call
  is made in this sandbox — confirmed by the "skipping" stderr line and by the run finishing in
  37ms with 19 tests skipped, not hung/timed out.
- `metrics.test.ts` — table-driven, covers every case the AC names: zero burn+zero accrual
  (∞), zero accrual (finite), accrual > burn (∞), accrual == burn (∞), the ε floor (default and
  overridden), a value lossy in IEEE-754 to prove no float touches the math, and
  `low_confidence` at 0h / 5.99h / 6h / 24h.
- `decimal.test.ts` — direct coverage of the arithmetic module (not one of the ticket's named
  "Tests required" files, added anyway given the audit focus is explicitly "Float drift").
- `sqlite/store.test.ts` — SQLite-specific: schema-on-first-open incl. nested dir creation,
  idempotent reopen, FK pragma enforcement, and a dedicated **AC4** test that strips every
  `SUPABASE_*` var from `process.env`, opens a sqlite ledger, and completes an insert.
- `index.ts` — re-exports all of the above from `@orbio-treasurer/core`.

Design choices worth flagging for audit:
- **Id generation is client-side for both dialects** (not Postgres's `gen_random_uuid()`
  default), specifically to close the "dialect-specific behaviour leaking above the interface"
  risk named in the ticket's Audit focus.
- **ε has no PRD-given number.** FR-1.3 states the formula but never a value. Defaulted to
  `0.01` USD/day, overridable per call; see Discovered.
- Money/token fields are typed `string` everywhere in `types.ts` (never `number`) — the LedgerStore
  interface itself makes passing a float a type error, on top of `decimal.ts` rejecting a
  malformed string at the repository boundary.

### Fixes after audit 1

Addressed `tasks/reports/T-011-audit-1.md`:

- **B1 (Blocker) fixed.** `postgres/store.ts`: added `fromJsonb()` (parses a jsonb column's
  string value back to JS; passes through if it's ever already parsed) and wired it into the
  four sites the auditor named — `mapAgentRow.policy`, `mapDecisionRow.inputs/action/result`,
  `mapBookSnapshotRow.view`. Verified against a real local Postgres cluster (see Evidence): the
  2 previously-failing conformance tests (`decisions: insert round-trips...`,
  `book_snapshots: insert round-trips...`) now pass; 19/19 conformance tests green.
- **M1 fixed.** Removed the unused `nowUtcIso()` export from `util.ts` (and its now-unused
  `IsoTimestamp` import). Nothing else referenced it (grepped; the only other hit was the stale
  compiled `dist/*.d.ts`, which rebuilds on the next `pnpm build`).
- **M2 addressed.** Kept `LedgerStore.dialect` — it's genuinely useful as a debug/logging tag —
  but added a doc comment on the interface stating explicitly that application code must never
  branch on it, so a future ticket doesn't grow the real version of the leak the auditor flagged.
- **Q1 (ε default) left as-is** per instruction — `0.01` USD/day, still flagged below for So.
- **Corrected the false "untestable here" claim** (Build notes and Discovered, struck through
  above) — a local Postgres cluster needs no outbound network and works fine in this sandbox;
  see Evidence for the real run.

## Discovered

- **No default ε value in the PRD.** FR-1.3 gives `max(burn − accrual, ε)` but never a number.
  Picked `DEFAULT_EPSILON_USD_PER_DAY = '0.01'` (exported, overridable). So: confirm this is a
  reasonable floor for the reference agent's scale, or give me a number.
- **`openPostgresLedger` takes a bare connection string**, not `SUPABASE_URL` +
  `SUPABASE_SERVICE_ROLE_KEY` / `DATABASE_URL` from `env.ts`. Turning the env shape into a
  connection string is env-wiring that belongs to whatever ticket wires `createTreasurer()` to
  a store (not yet ticketed as far as I can see) — flagging so it isn't assumed done.
- **PostgresLedgerStore does not apply migrations.** It assumes `supabase/migrations/*.sql` is
  already applied (via `pnpm db:migrate`), matching how a real hosted deployment works. The
  conformance suite applies them itself in its own `beforeAll`, same pattern as
  `schema.postgres.test.ts`.
- ~~T-011's Postgres path is untested by me in this sandbox — outbound TCP 5432 is blocked here
  by design.~~ **Corrected after audit 1: this was wrong.** CLAUDE.md's instruction only rules
  out reaching a *remote* database from this sandbox — a **local** Postgres cluster
  (`initdb`/`pg_ctl` on `127.0.0.1:55432`, no outbound network) needs no such connection, and
  `tasks/T-002.md` already documents standing one up here. The auditor did exactly that and
  found a real Blocker (B1, jsonb columns — see "Fixes after audit 1" below and
  `tasks/reports/T-011-audit-1.md`). I've since fixed B1 and re-verified against a real local
  cluster myself: 253/253 tests pass, 0 skipped, including all 19 conformance tests and all 20
  `schema.postgres.test.ts` tests. See Evidence.

## Blocked on
_(none — not blocked; ε default flagged above under Discovered, not a stop-the-line question)_

## Audit report

Pass 1 (fresh Sonnet session): `pnpm lint/typecheck/test` reproduced exactly as claimed
(211/253, 42 skipped). AC2/AC4 and all §4 checks green. **AC1/AC3 "both stores" claim was
unverified** — the ticket says Postgres was only checked structurally because the sandbox
"blocks TCP 5432," but a **local** cluster needs no outbound network and T-002's own audit
already used exactly that recipe here. I stood one up (`initdb`/`pg_ctl` on `127.0.0.1:55432`)
and ran the real Postgres conformance suite.

- **1 Blocker (B1)**: `PostgresLedgerStore` never `JSON.parse`s `jsonb` columns
  (`decisions.inputs/action/result`, `book_snapshots.view`, `agents.policy`) —
  `SqliteLedgerStore` does. Confirmed live: 2/19 Postgres conformance tests fail
  (decisions round-trip, book_snapshots round-trip); 17/19 pass, incl. AC3's money-6dp
  byte-identical check, which does hold. Exactly the "dialect-specific behaviour leaking above
  the interface" this ticket's own Audit focus names.
- 2 Minor: dead `nowUtcIso()` export (M1); `LedgerStore.dialect` field exposes dialect info
  above the interface, unused today but worth watching (M2).
- 1 Question: ε default (`0.01` USD/day) — PRD gives no number (confirmed by grep); reasonable
  low-stakes default, not blocking.
- Float drift / UTC / CLAUDE.md #4 & #5c: all MET, verified live including against real
  Postgres (numeric columns confirmed returned as strings, never JS numbers).

Full report: `tasks/reports/T-011-audit-1.md`. Status → in-code (Blocker outstanding).

### Audit pass 2

Fix commit reviewed: `9984e62` "fix(ledger): parse jsonb in Postgres store, audit-1 minors
[T-011]". Diff is tight — only `postgres/store.ts`, `types.ts`, `util.ts`, and `tasks/T-011.md`;
nothing out of scope.

- **B1: fixed, re-verified live.** Stood up a fresh local Postgres 16 cluster (same recipe as
  pass 1, `127.0.0.1:55434`, no outbound network) and ran `TEST_DATABASE_URL=... pnpm
  --filter @orbio-treasurer/core test`: **253/253 pass, 0 skipped** (up from 251/253 in pass 1).
  Verbose run confirms the two previously-failing tests by name now pass in the postgres
  branch: `decisions: insert round-trips inputs/action/result...` and `book_snapshots: insert
  round-trips...`. Also manually round-tripped `agents.policy` with a non-null value (the one
  jsonb path no existing test exercises) directly against the built store — returns a parsed
  object, not a string, both on insert and on a subsequent `getAgent`.
- **M1: fixed.** `nowUtcIso()` removed from `util.ts`; grepped the repo, zero remaining
  references outside the stale `dist/*.d.ts` (rebuilds on next build).
- **M2: addressed.** `LedgerStore.dialect` kept (legitimately useful as a debug tag) with a
  doc comment now stating application code must never branch on it. Reasonable resolution, not
  re-opening.
- Q1 (ε default) correctly left open for So, as instructed — not a fix-pass item.
- No regressions: re-ran without `TEST_DATABASE_URL` afterward — back to 211 passed / 42
  skipped, identical to pass 1's pre-fix baseline. `pnpm lint && pnpm typecheck` still green.

Full report: `tasks/reports/T-011-audit-1.md` (pass 2 appended). Status → in-test.

## Test report
_(tester — tests added, pnpm test / pnpm smoke results, untestable ACs and why)_

## Evidence

`pnpm lint && pnpm typecheck && pnpm test` all green at HEAD after this ticket. Core test count:
152 passed / 23 skipped (175) before → 211 passed / 42 skipped (253) after — net +59 passed,
+19 skipped (the 19 Postgres conformance tests, registered but skipping cleanly here).

```
@orbio-treasurer/core:test:  ✓ src/ledger/schema.sqlite.test.ts (20 tests) 584ms
@orbio-treasurer/core:test:  ✓ src/ledger/sqlite/store.test.ts (4 tests) 142ms
@orbio-treasurer/core:test:  ✓ src/ledger/t002.tester.test.ts (12 tests | 3 skipped) 122ms
@orbio-treasurer/core:test:  ✓ src/t003.tester.test.ts (25 tests) 100ms
@orbio-treasurer/core:test: stderr | src/ledger/ledger-conformance.test.ts
@orbio-treasurer/core:test:   ledger-conformance.test.ts: TEST_DATABASE_URL is not set — skipping
    the Postgres ledger conformance suite. See tasks/T-002.md for how to run it against a local
    cluster.
@orbio-treasurer/core:test:  ✓ src/ledger/ledger-conformance.test.ts (38 tests | 19 skipped) 37ms
@orbio-treasurer/core:test:  ✓ src/redact.test.ts (60 tests) 26ms
@orbio-treasurer/core:test:  ✓ src/log.test.ts (24 tests) 17ms
@orbio-treasurer/core:test:  ✓ src/env.test.ts (12 tests) 14ms
@orbio-treasurer/core:test:  ✓ src/ledger/decimal.test.ts (17 tests) 9ms
@orbio-treasurer/core:test:  ✓ src/ledger/metrics.test.ts (19 tests) 8ms
@orbio-treasurer/core:test:  ✓ src/no-console-log.test.ts (1 test) 5ms
@orbio-treasurer/core:test:  ✓ src/index.test.ts (1 test) 3ms
@orbio-treasurer/core:test:  ↓ src/ledger/schema.postgres.test.ts (20 tests | 20 skipped)
@orbio-treasurer/core:test: Test Files  12 passed | 1 skipped (13)
@orbio-treasurer/core:test:      Tests  211 passed | 42 skipped (253)
```

`pnpm typecheck` at the repo root also runs `@orbio-treasurer/core:build` (tsc emit) as its
dependency and that succeeds — `dist/` builds cleanly with the new exports.

Without `TEST_DATABASE_URL` set, `ledger-conformance.test.ts`'s Postgres branch collects the
same 19 test names under `ledger conformance — postgres` and shows them all `↓` (skipped) with
the `TEST_DATABASE_URL is not set` message, finishing in ~37-39ms (no attempted connection, no
hang). **This is no longer the only evidence** — see "### Fixes after audit 1" below for the
real run against a local Postgres cluster (19/19 conformance + 20/20 schema.postgres.test.ts,
253/253 total).

AC4 evidence: `src/ledger/sqlite/store.test.ts` → "AC4: the kit boots with LEDGER=sqlite and no
SUPABASE_* variable" — deletes every `SUPABASE_*` var from `process.env`, opens a fresh sqlite
ledger file, inserts an agent, asserts success; passes (see run above).

### Real Postgres run (audit-1 fix verification)

Local cluster, same recipe as `tasks/T-002.md`'s own audit trail (no outbound network — loopback
only):

```
mkdir -p /tmp/orbio_pg_test && chown -R postgres:postgres /tmp/orbio_pg_test
su postgres -c "/usr/lib/postgresql/16/bin/initdb -D /tmp/orbio_pg_test/data --auth=trust"
su postgres -c "/usr/lib/postgresql/16/bin/pg_ctl -D /tmp/orbio_pg_test/data \
  -l /tmp/orbio_pg_test/log -o '-p 55432 -k /tmp/orbio_pg_test/data' -w start"
su postgres -c "/usr/lib/postgresql/16/bin/createdb -h 127.0.0.1 -p 55432 -U postgres orbio_treasurer_test"
su postgres -c "psql -h 127.0.0.1 -p 55432 -U postgres -d orbio_treasurer_test -c \"create role anon nologin;\""

TEST_DATABASE_URL=postgres://postgres@127.0.0.1:55432/orbio_treasurer_test \
  pnpm --filter @orbio-treasurer/core test
```

Result — full core suite, real Postgres, **0 skipped**:
```
 Test Files  13 passed (13)
      Tests  253 passed (253)
   Duration  4.58s
```

Verbose run of just the Postgres-dependent files, confirming the B1 fix specifically:
```
TEST_DATABASE_URL=postgres://postgres@127.0.0.1:55432/orbio_treasurer_test \
  pnpm --filter @orbio-treasurer/core exec vitest run --reporter=verbose \
  src/ledger/ledger-conformance.test.ts src/ledger/schema.postgres.test.ts src/ledger/t002.tester.test.ts

 ✓ ledger conformance — postgres > decisions: insert round-trips inputs/action/result and the executed/public flags 2ms
 ✓ ledger conformance — postgres > book_snapshots: insert round-trips (no agent_id — global book state) 1ms
 ✓ ledger conformance — postgres > agents: insertAgent -> getAgent / getAgentBySlug round-trip; UTC timestamps; defaults applied 12ms
 ... (all 19 "ledger conformance — postgres" tests ✓, same names as the sqlite run)
 ✓ schema.postgres.test.ts > ... (all 20 tests ✓, AC1/AC2/AC3/AC4)

 Test Files  3 passed (3)
      Tests  70 passed (70)
```

Cluster torn down afterward:
```
su postgres -c "/usr/lib/postgresql/16/bin/pg_ctl -D /tmp/orbio_pg_test/data -w stop"
# waiting for server to shut down.... done / server stopped
rm -rf /tmp/orbio_pg_test
```

Re-ran `pnpm lint && pnpm typecheck && pnpm test` afterward with `TEST_DATABASE_URL` unset again
— back to 211 passed / 42 skipped (253), same as before the fix, confirming the skip path is
unaffected by the jsonb fix.

Files touched (git status, all under `packages/core/src/`):
```
 M packages/core/src/index.ts
?? packages/core/src/ledger/conformance-suite.ts
?? packages/core/src/ledger/decimal.test.ts
?? packages/core/src/ledger/decimal.ts
?? packages/core/src/ledger/ledger-conformance.test.ts
?? packages/core/src/ledger/metrics.test.ts
?? packages/core/src/ledger/metrics.ts
?? packages/core/src/ledger/postgres/store.ts
?? packages/core/src/ledger/sqlite/store.test.ts
?? packages/core/src/ledger/sqlite/store.ts
?? packages/core/src/ledger/types.ts
?? packages/core/src/ledger/util.ts
```

## Sign-off
_(So: date + "ok" or what to change)_
