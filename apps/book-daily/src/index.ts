/** Orbio Book Daily — the reference agent. Regenerated from the kit's x-bot template in T-027. */
import { CORE_VERSION } from '@orbio-treasurer/core';

console.error(`book-daily shell · core ${CORE_VERSION} · agent loop lands in T-024`);
