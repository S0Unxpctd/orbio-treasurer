# Orbio Treasurer

**Give any AI agent a treasury that funds its own inference from a $ORBIO position, and prove it in public.**

Built for Orbio Build Week (September 2026). Status: docs phase, see `STATUS.md`.

- `PRD.md` — what we build and why (source of truth)
- `ARCHITECTURE.md` — how, stack locked
- `PROCESS.md` — Code → Audit → Test loop for vibecoding with a coding agent
- `CLAUDE.md` — instructions the coding agent reads first
- `tasks/` — the ticket board (`tasks/README.md`) and one file per ticket
- `docs/api-notes.md` — everything learned about Orbio's real API, dated
- `docs/runbook.md` — deploy, secrets, go-live checklist, rollback
- `adr/` — architecture decisions

Code lands in `packages/` and `apps/` starting with ticket T-001.
